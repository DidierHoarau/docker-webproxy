{
  "nginx": {
    "configDir": "/etc/nginx/conf.d",
    "reloadCommand": "/usr/sbin/nginx -s reload",
    "wellKnownDir": "/opt/data/certificates/well-known",
    "letsencryptDir": "/opt/data/certificates/letsencrypt"
  }
}
